import React from "react";

const Reserves = ({ user }) => {
  
  return (
    <div
      style={{
        margin: "20px",
      }}
    >
      <h1>Reserves</h1>
    </div>
  );
};

export default Reserves;
